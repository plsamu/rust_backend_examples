# some backend examples with rust

## utils

```bash
git config --list
git config --global user.email "email"
git config --global user.name "name"

# https://ghp_XXXXXXXXX@github.com/user/proj.git
git remote set-url origin new_url
```
